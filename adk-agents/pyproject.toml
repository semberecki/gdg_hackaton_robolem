[project]
name = "triz-agent"
version = "0.1.0"
description = "ADK Agent to orchestrate LLM calls and TRIZ MCP tools"
requires-python = ">=3.13"
dependencies = [
    "google-adk>=2.0.0",
    "mcp>=1.2.0",
    "aiohttp>=3.9.0",
]
