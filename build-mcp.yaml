steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'us-central1-docker.pkg.dev/r2d2-00/buildwithai-repo/triz-mcp-server:latest',
      './mcp-server'
    ]
    env: ['DOCKER_BUILDKIT=1']
images:
  - 'us-central1-docker.pkg.dev/r2d2-00/buildwithai-repo/triz-mcp-server:latest'
