steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-f', 'apps/frontend/Dockerfile',
      '-t', 'us-central1-docker.pkg.dev/grand-citadel-501407-n4/buildwithai-repo/buildwithai-frontend:latest',
      '.'
    ]
    env: ['DOCKER_BUILDKIT=1']
images:
  - 'us-central1-docker.pkg.dev/grand-citadel-501407-n4/buildwithai-repo/buildwithai-frontend:latest'
